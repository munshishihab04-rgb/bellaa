{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "cd artifacts/rusoftshop && npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "artifacts/rusoftshop/dist",
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile"
}
